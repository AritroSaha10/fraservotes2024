extend type Query {
  encryptedBallots: [Ballot!]!
  ballotCount: Int!
}

extend type Mutation {
  submitBallot(encryptedBallot: String): Void
  deleteBallots: Void
}

type Ballot {
  encryptedBallot: String!
}
