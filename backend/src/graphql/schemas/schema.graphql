schema {
    query: Query
    mutation: Mutation # Uncomment once you have mutations
}

type Query

type Mutation

# TODO: Maybe import a scalar or create a custom one for a void type
type Void {
    void: Boolean
}
