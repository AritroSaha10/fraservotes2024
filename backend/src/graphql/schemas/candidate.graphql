extend type Query {
    candidates: [Candidate!]!
    candidate(id: ID!): Candidate
}

type Candidate {
    _id: ID!
    fullName: String!
    position: Position!
    grade: Int!
    picture: String!
}
